# Copy to web/.env and fill in. The dev server reads these server-side only;
# they are never bundled into the browser.
OPENROUTER_API_KEY=
# Optional override; defaults to anthropic/claude-sonnet-4.6
OPENROUTER_MODEL=anthropic/claude-sonnet-4.6
